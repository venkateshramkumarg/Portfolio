import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = { 
  title: "Venkatesh G",
  description: "Full-stack developer proficient in React, TypeScript, Prisma, and modern JavaScript frameworks, seeking a challenging role to build exceptional web applications.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
